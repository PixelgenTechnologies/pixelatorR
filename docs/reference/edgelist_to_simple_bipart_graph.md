# Create a simple bipartite graph from an edgelist

Create a simple bipartite graph from an edgelist

## Usage

``` r
edgelist_to_simple_bipart_graph(edgelist)
```

## Arguments

- edgelist:

  An object of class `tbl_graph`

## Value

An object of class `tbl_graph` containing a bipartite graph
