# The CellGraph class

The CellGraph class is designed to hold information needed for working
with mpx single-cell graphs.

## Slots

- `cellgraph`:

  A `tbl_graph` object corresponding to a cell graph

- `counts`:

  A `matrix`-like object with marker counts

- `layout`:

  A `list` of `tbl_df` objects with coordinates for cell layouts
