# Calculate cosine distances between two sets of coordinates

Calculate cosine distances between two sets of coordinates

## Usage

``` r
cos_dist(A, B)
```

## Arguments

- A, B:

  Matrices of coordinates. Each row corresponds to a point in space.
