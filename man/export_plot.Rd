% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-utils.R
\name{export_plot}
\alias{export_plot}
\title{Save a ggplot2 plot to one or more file formats with options for size and directory creation.}
\usage{
export_plot(
  filename,
  plot = ggplot2::last_plot(),
  width = 10,
  height = 10,
  create_dir = TRUE,
  file_formats = c("png", "pdf"),
  overwrite = getOption("export_plot.overwrite", FALSE)
)
}
\arguments{
\item{filename}{Character. The path and base filename (without extension) for saving the plot.}

\item{plot}{ggplot object. The plot to save. Defaults to the last plot produced (\code{last_plot()}).}

\item{width}{Numeric. The width of the plot in inches. Default is 10.}

\item{height}{Numeric. The height of the plot in inches. Default is 10.}

\item{create_dir}{Logical. Whether to create the directory if it does not exist. Default is TRUE.}

\item{file_formats}{Character vector. File formats to save to (e.g., "png", "pdf"). Default is c("png", "pdf").}

\item{overwrite}{Logical. Whether to overwrite existing files. Default is taken from the global option
"export_plot.overwrite" (default FALSE).}
}
\value{
Invisibly returns NULL. Used for its side effect of writing plot files.
}
\description{
This function is a wrapper around \code{ggsave()} that allows for saving the same plot in multiple formats and handles
directory creation if needed. It also includes an option to skip saving if the file already exists, based on a
global option.
}
\examples{
\dontrun{
export_plot("results/myplot", plot = myplot, width = 8, height = 6)
}
# Change overwrite behavior globally
options(export_plot.overwrite = TRUE)

# Now existing files will be overwritten without needing to specify overwrite in the function call
\dontrun{
export_plot("results/myplot", plot = myplot, width = 8, height = 6)
}

}
