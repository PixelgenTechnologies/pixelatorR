% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/PNAAssay.R
\name{as.PNAAssay5}
\alias{as.PNAAssay5}
\alias{as.PNAAssay5.Assay5}
\title{Convert objects to a \code{\link{PNAAssay5}}}
\usage{
as.PNAAssay5(x, ...)

\method{as.PNAAssay5}{Assay5}(x, cellgraphs = NULL, proximity = NULL, fs_map = NULL, ...)
}
\arguments{
\item{x}{An object to convert to class \code{\link{PNAAssay5}}}

\item{...}{Arguments passed to other methods}

\item{cellgraphs}{A list of \code{\link{CellGraph}} objects}

\item{proximity}{A \code{tbl_df} with proximity scores}

\item{fs_map}{A \code{tbl_df} with information on source PXL file
paths, sample IDs, and component IDs}
}
\value{
A \code{PNAAssay5} object
}
\description{
Convert objects to a \code{\link{PNAAssay5}}
}
\examples{
library(pixelatorR)
library(dplyr)
library(SeuratObject)

pxl_file <- minimal_pna_pxl_file()
counts <- ReadPNA_counts(pxl_file)
assay <- CreateAssay5Object(
  counts = counts
)
pna_assay5 <- as.PNAAssay5(
  assay,
  cellgraphs = rep(list(NULL), ncol(counts)) \%>\%
    setNames(colnames(counts))
)
pna_assay5

}
\concept{assay}
