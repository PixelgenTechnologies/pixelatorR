% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_markers.R
\name{FilterMarkers}
\alias{FilterMarkers}
\title{Filter markers by isotype-relative and/or absolute abundance}
\usage{
FilterMarkers(
  object,
  isotype_markers,
  isotype_ratio = 1.5,
  abundance_threshold = NULL,
  min_cell_fraction = 0.05,
  group_column = NULL,
  return_stats = FALSE
)
}
\arguments{
\item{object}{A \code{Seurat} object with a \code{counts} layer.}

\item{isotype_markers}{Character vector of isotype control marker names
(for example \code{c("mIgG1", "mIgG2a", "mIgG2b")}.}

\item{isotype_ratio}{Numeric relative cutoff versus the median isotype CPM,
or \code{NULL} to skip this cutoff. Default is \code{1.5}.}

\item{abundance_threshold}{Numeric absolute CPM cutoff, or \code{NULL} to skip
this cutoff. Default is \code{NULL}.}

\item{min_cell_fraction}{Minimum fraction of cells that must be positive
for a marker to be kept. Default is \code{0.05}.}

\item{group_column}{Optional metadata column name. If provided, the
function returns a named list with one result per group.}

\item{return_stats}{Logical; if \code{TRUE}, return a tibble of per-marker
statistics (including a \code{kept} column) instead of marker names.}
}
\value{
If \code{group_column} is \code{NULL}: a character vector of kept marker names,
or a tibble if \code{return_stats} is \code{TRUE}. If \code{group_column} is set: a
named list of the same, one element per group.
}
\description{
Identifies markers that are positive in a sufficient fraction of cells.
A cell is positive for a marker if that cell's counts-per-million (CPM)
clears \strong{every} cutoff that is supplied:
}
\details{
\itemize{
\item If \code{isotype_ratio} is set, CPM must be greater than
\code{isotype_ratio} times the median CPM of \code{isotype_markers}.
\item If \code{abundance_threshold} is set, CPM must be greater than
\code{abundance_threshold}.
\item If both are set, both conditions must hold (AND). Neither argument
overrides the other. At least one cutoff is required.
}

A marker is kept if at least \code{min_cell_fraction} of cells are positive.
Isotype controls are always dropped from the result.

When \code{group_column} is set, the isotype median and whether each cell is
positive are computed independently in each group.
}
\examples{

library(pixelatorR)

seur <-
  ReadPNA_Seurat(
    minimal_pna_pxl_file(),
    overwrite = TRUE,
    load_proximity_scores = FALSE,
    verbose = FALSE
  )

# Keep markers above 1.5 times the isotype median CPM in at least 5\% of cells
kept <- FilterMarkers(
  object = seur,
  isotype_markers = c("mIgG1", "mIgG2a", "mIgG2b")
)

# Require both the isotype ratio and an absolute CPM floor
kept_and <- FilterMarkers(
  object = seur,
  isotype_markers = c("mIgG1", "mIgG2a", "mIgG2b"),
  isotype_ratio = 1.5,
  abundance_threshold = 2000,
  min_cell_fraction = 0.05
)

# Per-group filtering
seur$sample <- c("S1", "S1", "S2", "S2", "S2")
kept_by_sample <- FilterMarkers(
  object = seur,
  isotype_markers = c("mIgG1", "mIgG2a", "mIgG2b"),
  group_column = "sample"
)

}
