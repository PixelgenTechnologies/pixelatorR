% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_rotating_layout.R
\name{apply_heuristic_lighting}
\alias{apply_heuristic_lighting}
\title{Compute heuristic illumination for 3D layouts}
\usage{
apply_heuristic_lighting(
  layout,
  clamp_quantiles = c(0.01, 0.95),
  directional_light_weight = 0.7,
  volume_shading_weight = 0.5,
  ambient_occlusion_weight = 1,
  ambient_occlusion_k = 20,
  normalize_weights = TRUE
)
}
\arguments{
\item{layout}{A data frame or tibble with numeric columns \code{x}, \code{y}, and \code{z}.}

\item{clamp_quantiles}{Numeric vector of length 2 in \verb{[0, 1]}. Illumination is
clamped to these quantiles to reduce outlier influence. Default: \code{c(0.01, 0.95)}.}

\item{directional_light_weight}{Non-negative numeric scalar. Weight for directional
light component. Default: \code{0.7}.}

\item{volume_shading_weight}{Non-negative numeric scalar. Weight for radial volume
shading component. Default: \code{0.5}.}

\item{ambient_occlusion_weight}{Non-negative numeric scalar. Weight for ambient
occlusion component. Default: \code{1}.}

\item{ambient_occlusion_k}{Positive integer. Number of nearest neighbors used for
ambient occlusion approximation. Default: \code{20}.}

\item{normalize_weights}{Logical; if \code{TRUE}, weights are normalized to sum to 1.
Default: \code{TRUE}.}
}
\value{
A numeric vector of illumination values (length \code{nrow(layout)}). Higher values indicate stronger
illumination.
}
\description{
Combines three simple lighting heuristics for 3D coordinates:
(1) directional light from the positive z-axis,
(2) radial volume shading from the origin,
and (3) ambient occlusion approximated from mean distance to nearest neighbors.
}
\examples{
set.seed(1)
layout <- tibble::tibble(
  x = rnorm(100),
  y = rnorm(100),
  z = rnorm(100)
)
illum <- apply_heuristic_lighting(layout)
layout_with_illum <- apply_heuristic_lighting(layout, return_layout = TRUE)

}
