% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{ReadMPX_item}
\alias{ReadMPX_item}
\alias{ReadMPX_polarization}
\alias{ReadMPX_colocalization}
\alias{ReadMPX_edgelist}
\title{Read an MPX data item}
\usage{
ReadMPX_item(
  filename,
  items = c("colocalization", "polarization", "edgelist"),
  verbose = TRUE
)

ReadMPX_polarization(filename, verbose = TRUE)

ReadMPX_colocalization(filename, verbose = TRUE)

ReadMPX_edgelist(filename, verbose = TRUE)
}
\arguments{
\item{filename}{Path to a .pxl file}

\item{items}{One or several of "colocalization", "polarization", "edgelist"}

\item{verbose}{Print messages}
}
\value{
Either an object of class \code{tbl_df} or a list of \code{tbl_df}
objects if multiple \code{items} are selected
}
\description{
\code{ReadMPX_item} reads any number of items from a .pxl file. If multiple
items are specified, the output is a list of  \code{tbl_df} objects.
Otherwise the output is a single \code{tbl_df}. \code{ReadMPX_polarization},
\code{ReadMPX_colocalization} and \code{ReadMPX_edgelist} are wrappers for
\code{ReadMPX_item} to read specific items.
}
\examples{

library(pixelatorR)

# Load example data
pxl_file <- system.file("extdata/five_cells",
  "five_cells.pxl",
  package = "pixelatorR"
)
polarization <- ReadMPX_item(pxl_file, items = "polarization")
polarization

# Alternative 2
polarization <- ReadMPX_polarization(pxl_file)

}
\seealso{
Other data-loaders: 
\code{\link{ReadMPX_Seurat}()}
}
\concept{data-loaders}
