% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/compute_lps.R
\docType{methods}
\name{ComputeLPS}
\alias{ComputeLPS}
\alias{ComputeLPS.CellGraph}
\alias{ComputeLPS.CellGraphList}
\alias{ComputeLPS.PNAAssay}
\alias{ComputeLPS.PNAAssay5}
\alias{ComputeLPS.Seurat}
\title{Compute local proximity scores}
\usage{
ComputeLPS(object, ...)

\method{ComputeLPS}{CellGraph}(
  object,
  markers = NULL,
  method = c("analytical", "permutation"),
  mode = c("self-clustering", "all", "any"),
  iterations = 50L,
  k = 3L,
  A_k = NULL,
  seed = 123,
  name = "lps",
  ...
)

\method{ComputeLPS}{CellGraphList}(
  object,
  markers = NULL,
  method = c("analytical", "permutation"),
  mode = c("self-clustering", "all", "any"),
  iterations = 50L,
  k = 3L,
  seed = 123,
  name = "lps",
  verbose = TRUE,
  cl = NULL,
  ...
)

\method{ComputeLPS}{PNAAssay}(
  object,
  markers = NULL,
  method = c("analytical", "permutation"),
  mode = c("self-clustering", "all", "any"),
  iterations = 50L,
  k = 3L,
  seed = 123,
  name = "lps",
  verbose = TRUE,
  cl = NULL,
  ...
)

\method{ComputeLPS}{PNAAssay5}(
  object,
  markers = NULL,
  method = c("analytical", "permutation"),
  mode = c("self-clustering", "all", "any"),
  iterations = 50L,
  k = 3L,
  seed = 123,
  name = "lps",
  verbose = TRUE,
  cl = NULL,
  ...
)

\method{ComputeLPS}{Seurat}(
  object,
  assay = NULL,
  markers = NULL,
  method = c("analytical", "permutation"),
  mode = c("self-clustering", "all", "any"),
  iterations = 50L,
  k = 3L,
  seed = 123,
  name = "lps",
  verbose = TRUE,
  cl = NULL,
  ...
)
}
\arguments{
\item{object}{An object}

\item{...}{Additional parameters passed to other methods}

\item{markers}{A character vector specifying the markers to use. If
\code{NULL}, all markers in the count matrix of each \code{CellGraph}
are used. Methods that iterate over multiple \code{CellGraph} objects
keep the intersection with available markers. If none of the requested
markers are present in a graph, a warning is emitted and that graph is
left unmodified.}

\item{method}{A character string specifying the method to use for
computing the local proximity score. Options are \code{"analytical"}
or \code{"permutation"}.}

\item{mode}{A character string specifying the mode of computation.
See \code{\link{local_proximity}} for details. Default is
\code{"self-clustering"}.}

\item{iterations}{An integer specifying the number of iterations to run
when \code{method = "permutation"}.}

\item{k}{An integer specifying the neighborhood size to consider.}

\item{A_k}{An optional pre-computed expanded adjacency matrix. Only
used by the \code{CellGraph} method.}

\item{seed}{An integer for random seed setting.}

\item{name}{Name of the layer (when a matrix is returned) or metadata
column (when a vector is returned) used to store scores. Default is
\code{"lps"}.}

\item{verbose}{Print messages}

\item{cl}{An integer to indicate number of child-processes (integer values
are ignored on Windows) for parallel evaluations. See Details on performance
in the documentation for \code{pbapply}. The default is \code{NULL},
which means that no parallelization is used.}

\item{assay}{Name of assay to compute local proximity scores for}
}
\value{
An object with local proximity scores stored on each
\code{\link{CellGraph}}. Matrix results are stored as a layer;
vector results are stored in node \code{meta.data}.
}
\description{
Computes local proximity scores (LPS) for nodes in PNA cell graphs
using \code{\link{local_proximity}} and stores the result on each
\code{\link{CellGraph}}.

Computes local proximity scores for nodes in PNA cell graphs using
\code{\link{local_proximity}} and stores the output on each
\code{\link{CellGraph}}.
}
\details{
The default \code{mode} is \code{"self-clustering"}, which returns a
node-by-marker matrix that is stored as a layer. Other modes return a
single score per node, which is stored in \code{meta.data}.
}
\examples{
library(pixelatorR)

se <- ReadPNA_Seurat(minimal_pna_pxl_file(), verbose = FALSE)
se <- LoadCellGraphs(se, cells = colnames(se)[1], verbose = FALSE)
cg <- CellGraphs(se)[[1]]

# Matrix result is stored as a layer
cg <- ComputeLPS(cg, markers = "B2M")
SeuratObject::Layers(cg)

# Vector result is stored in node metadata
cg <- ComputeLPS(cg, markers = "B2M", mode = "all", name = "lps_b2m")
head(CellGraphData(cg, slot = "meta.data"))

# Compute LPS for loaded cell graphs in a PNAAssay
pna_assay <- ComputeLPS(se[["PNA"]], markers = "B2M")

# Seurat method (only loaded cell graphs are processed)
seur <- ComputeLPS(se, markers = "B2M")

}
