% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/CellGraphAssay.R, R/PNAAssay.R,
%   R/objects.R
\name{FSMap}
\alias{FSMap}
\alias{FSMap<-}
\alias{FSMap.MPXAssay}
\alias{FSMap<-.MPXAssay}
\alias{FSMap.PNAAssay}
\alias{FSMap.PNAAssay5}
\alias{FSMap<-.PNAAssay}
\alias{FSMap<-.PNAAssay5}
\alias{FSMap.Seurat}
\alias{FSMap<-.Seurat}
\title{FS map}
\usage{
FSMap(object, ...)

FSMap(object, ...) <- value

\method{FSMap}{MPXAssay}(object, ...)

\method{FSMap}{MPXAssay}(object, ...) <- value

\method{FSMap}{PNAAssay}(object, ...)

\method{FSMap}{PNAAssay5}(object, ...)

\method{FSMap}{PNAAssay}(object, ...) <- value

\method{FSMap}{PNAAssay5}(object, ...) <- value

\method{FSMap}{Seurat}(object, ...)

\method{FSMap}{Seurat}(object, ...) <- value
}
\arguments{
\item{object}{A \code{PNAAssay} or \code{PNAAssay5} object}

\item{...}{Additional arguments (not used)}

\item{value}{A new \code{tbl_df} to replace the current fs_map with}
}
\value{
\code{FSMap}: An \code{fs_map} tibble

\code{FSMap<-}: An object with an updated \code{fs_map} tibble
}
\description{
Get and set \code{fs_map} tibble for a \code{\link{CellGraphAssay}},
\code{\link{CellGraphAssay5}} or a \code{Seurat} object
}
\examples{
library(pixelatorR)
library(dplyr)

pxl_file <- minimal_pna_pxl_file()
seur_obj <- ReadPNA_Seurat(pxl_file)
FSMap(seur_obj[["PNA"]])

# If the PXL has been moved, we can update the fs_map
# Here we copy the test PXL file to a temporary location
# to illustrate how to update the fs_map
temp_file <- fs::file_temp(ext = "pxl")
fs::file_copy(path = pxl_file, temp_file)

# Change file path in fs_map
FSMap(seur_obj[["PNA"]]) <- FSMap(seur_obj[["PNA"]]) \%>\%
  mutate(pxl_file = temp_file)

# Now the fs_map has been updated with the correct path
FSMap(seur_obj[["PNA"]])

pxl_file <- system.file("extdata/five_cells",
  "five_cells.pxl",
  package = "pixelatorR"
)
seur_obj <- ReadMPX_Seurat(pxl_file)

# Check PXL file paths in a Seurat object
FSMap(seur_obj)

library(pixelatorR)

# Create example Seurat object
pxl_file <- minimal_pna_pxl_file()
seur_obj <- ReadPNA_Seurat(pxl_file)

# Replace FSMap in Seurat object
FSMap(seur_obj) <- FSMap(seur_obj) \%>\%
  mutate(pxl_file = fs::path_rel(pxl_file))

}
\seealso{
\code{\link[=CellGraphs]{CellGraphs()}} for getting/setting \code{\link{CellGraph}} lists
and \code{\link[=PolarizationScores]{PolarizationScores()}},\code{\link[=ColocalizationScores]{ColocalizationScores()}} for getting/setting
spatial metrics
}
