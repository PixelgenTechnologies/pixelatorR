% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CellGraphList.R
\name{CellGraphList-methods}
\alias{CellGraphList-methods}
\alias{print.CellGraphList}
\alias{[.CellGraphList}
\alias{[<-.CellGraphList}
\alias{[[<-.CellGraphList}
\alias{names<-.CellGraphList}
\alias{c.CellGraphList}
\alias{as.list.CellGraphList}
\alias{lapply.CellGraphList}
\title{CellGraphList Methods}
\usage{
\method{print}{CellGraphList}(x, ...)

\method{[}{CellGraphList}(x, i, ...)

\method{[}{CellGraphList}(x, i) <- value

\method{[[}{CellGraphList}(x, i) <- value

\method{names}{CellGraphList}(x) <- value

\method{c}{CellGraphList}(...)

\method{as.list}{CellGraphList}(x, ...)

\method{lapply}{CellGraphList}(X, FUN, ...)
}
\arguments{
\item{x, X}{A \code{\link{CellGraphList}} object}

\item{...}{Currently not used}

\item{i}{Index to extract or replace}

\item{value}{A \code{\link{CellGraph}}, \code{NULL}, or a list of those}

\item{FUN}{A function to apply to each element}
}
\value{
\code{[}, \code{[<-}, \code{[[<-}, \code{names<-}, \code{c}, and
\code{lapply}: a \code{CellGraphList}. \code{as.list}: a named list.
\code{print}: \code{x}, invisibly.
}
\description{
Methods for \code{\link{CellGraphList}} objects. Subsetting, concatenation,
and replacement type-check elements against \code{\link{CellGraph}}.
Unloaded graphs may be stored as \code{NULL}; \code{x[[i]] <- NULL} keeps
the name and stores \code{NULL} rather than dropping the element.
Use \code{lapply.CellGraphList} to apply a function without dropping the
\code{CellGraphList} class (\code{base::lapply} is not an S3 generic).
}
\section{Functions}{
\itemize{
\item \code{print(CellGraphList)}: Print a \code{CellGraphList}

\item \code{[}: Subset a \code{CellGraphList}. Unknown
character names raise an error.

\item \code{`[`(x, i) <- value}: Replace a subset of graphs. \code{NULL}
unloads the selected cells without dropping their names.

\item \code{`[[`(x, i) <- value}: Replace a single graph. \code{NULL}
unloads that cell without dropping its name.

\item \code{`names`(x) <- value}: Set names. Names must be unique and
non-missing.

\item \code{c(CellGraphList)}: Concatenate \code{CellGraphList} objects
with lists of \code{CellGraph} or \code{NULL}

\item \code{as.list(CellGraphList)}: Convert to a named list

\item \code{lapply(CellGraphList)}: Apply a function to each element and
return a \code{CellGraphList}

}}
\examples{
library(pixelatorR)
library(tidygraph)
library(dplyr)

edges <- tibble(from = c("a", "b"), to = c("b", "c"))
g <- as_tbl_graph(edges, directed = FALSE) \%N>\%
  mutate(node_type = c("umi1", "umi2", "umi1"))
attr(g, "type") <- "bipartite"
cg <- CreateCellGraphObject(cellgraph = g)
cgl <- CreateCellGraphList(list(cell_1 = cg, cell_2 = cg))

# Print and subset
print(cgl)
cgl[1]

# Unload a graph without dropping its name
cgl[[1]] <- NULL

# Concatenate and map while keeping the class
c(cgl[1], cgl[2])
lapply.CellGraphList(cgl, identity)

}
\seealso{
\code{\link{CellGraphList}}
}
\concept{cellgraph}
