% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/PNAAssay.R, R/objects.R
\name{ProximityScores}
\alias{ProximityScores}
\alias{ProximityScores<-}
\alias{ProximityScores.PNAAssay}
\alias{ProximityScores.PNAAssay5}
\alias{ProximityScores<-.PNAAssay}
\alias{ProximityScores<-.PNAAssay5}
\alias{ProximityScores.Seurat}
\alias{ProximityScores<-.Seurat}
\title{ProximityScores}
\usage{
ProximityScores(object, ...)

ProximityScores(object, ...) <- value

\method{ProximityScores}{PNAAssay}(
  object,
  add_marker_counts = FALSE,
  add_marker_proportions = FALSE,
  lazy = FALSE,
  calc_log2ratio = TRUE,
  ...
)

\method{ProximityScores}{PNAAssay5}(
  object,
  add_marker_counts = FALSE,
  add_marker_proportions = FALSE,
  lazy = FALSE,
  calc_log2ratio = TRUE,
  ...
)

\method{ProximityScores}{PNAAssay}(object, ...) <- value

\method{ProximityScores}{PNAAssay5}(object, ...) <- value

\method{ProximityScores}{Seurat}(
  object,
  assay = NULL,
  meta_data_columns = NULL,
  add_marker_counts = FALSE,
  add_marker_proportions = FALSE,
  lazy = FALSE,
  calc_log2ratio = TRUE,
  ...
)

\method{ProximityScores}{Seurat}(object, assay = NULL, ...) <- value
}
\arguments{
\item{object}{An object with polarization scores}

\item{...}{Not implemented}

\item{value}{A \code{tbl_df} or \code{tbl_lazy} with proximity scores}

\item{add_marker_counts}{A logical indicating whether to add marker
counts to the output ("count_1" and "count_2")}

\item{add_marker_proportions}{A logical indicating whether to add marker
count proportions to the output ("p1" and "p2")}

\item{lazy}{A logical indicating whether to lazy load the proximity scores
from the PXL files}

\item{calc_log2ratio}{A logical indicating whether to calculate the
log2 ratio proximity score}

\item{assay}{Name of a \code{PNAAssay}}

\item{meta_data_columns}{A character vector with meta.data column names.
This option can be useful to join meta.data columns with the proximity
score table.}
}
\value{
\code{ProximityScores}: Proximity scores

\code{ProximityScores<-}: An object with updated proximity scores
}
\description{
Get and set proximity scores for a \code{\link{PNAAssay}},
\code{\link{PNAAssay5}} or a \code{Seurat} object
}
\examples{
library(pixelatorR)
library(dplyr)

pxl_file <- minimal_pna_pxl_file()
seur_obj <- ReadPNA_Seurat(pxl_file)
ProximityScores(seur_obj[["PNA"]])

# Set proximity scores
ProximityScores(seur_obj[["PNA"]]) <-
  ProximityScores(seur_obj[["PNA"]]) \%>\%
  mutate(ratio = join_count / join_count_expected_mean)

library(pixelatorR)

# Create example Seurat object
pxl_file <- minimal_pna_pxl_file()
seur_obj <- ReadPNA_Seurat(pxl_file)

# Get proximity scores
proximity <- ProximityScores(seur_obj)
proximity

# Get proximity scores with additional meta data
proximity <-
  ProximityScores(seur_obj, meta_data_columns = c("n_umi", "n_edges"))
proximity

# Get proximity scores with marker_1 and marker_2 counts
proximity <-
  ProximityScores(seur_obj, add_marker_counts = TRUE)
proximity

# Update proximity scores in Seurat object
ProximityScores(seur_obj) <- ProximityScores(seur_obj) \%>\%
  mutate(ratio = join_count / join_count_expected_mean)

}
\seealso{
Other spatial metrics: 
\code{\link{ColocalizationScores}()},
\code{\link{Edgelists}()},
\code{\link{PolarizationScores}()}
}
\concept{spatial metrics}
