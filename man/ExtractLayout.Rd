% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/extract_layout.R
\docType{methods}
\name{ExtractLayout}
\alias{ExtractLayout}
\alias{ExtractLayout.CellGraph}
\alias{ExtractLayout.CellGraphList}
\alias{ExtractLayout.PNAAssay}
\alias{ExtractLayout.PNAAssay5}
\alias{ExtractLayout.Seurat}
\title{Extract graph layout coordinates}
\usage{
ExtractLayout(object, ...)

\method{ExtractLayout}{CellGraph}(
  object,
  layout_method = "wpmds_3d",
  vars = NULL,
  layer = NULL,
  ...
)

\method{ExtractLayout}{CellGraphList}(
  object,
  layout_method = "wpmds_3d",
  vars = NULL,
  cells = NULL,
  layer = NULL,
  ...
)

\method{ExtractLayout}{PNAAssay}(
  object,
  layout_method = "wpmds_3d",
  vars = NULL,
  cells = NULL,
  layer = NULL,
  ...
)

\method{ExtractLayout}{PNAAssay5}(
  object,
  layout_method = "wpmds_3d",
  vars = NULL,
  cells = NULL,
  layer = NULL,
  ...
)

\method{ExtractLayout}{Seurat}(
  object,
  layout_method = "wpmds_3d",
  vars = NULL,
  cells = NULL,
  assay = NULL,
  layer = NULL,
  ...
)
}
\arguments{
\item{object}{An object}

\item{...}{Additional parameters passed to other methods}

\item{layout_method}{Name of a stored layout, typically one computed with
\code{\link{ComputeLayout}} or loaded with \code{\link{LoadCellGraphs}}.
Default is \code{"wpmds_3d"}. The layout must contain \code{x}, \code{y},
and \code{z} coordinates.}

\item{vars}{Optional character vector of node-level variables to fetch with
\code{\link[SeuratObject]{FetchData}} (markers, metadata columns, graph
vertex attributes, or reduction embeddings). Missing values are filled with
\code{NA} rather than raising an error.}

\item{layer}{Name of a node matrix layer passed to
\code{\link[SeuratObject]{FetchData}}. \code{NULL} (default) uses the same
layer selection as \code{FetchData.CellGraph}.}

\item{cells}{Component IDs to extract. If \code{NULL}, all loaded
\code{\link{CellGraph}} objects are used. Unloaded graphs raise an error
when they are included in \code{cells}.}

\item{assay}{Name of assay to extract layouts from}
}
\value{
A \code{tbl_df} with columns \code{x}, \code{y}, \code{z} and any
requested \code{vars}. Methods that extract from multiple components also
include a \code{component} column.
}
\description{
Extracts a stored 3D layout from \code{\link{CellGraph}} objects and
optionally joins node-level variables retrieved with
\code{\link[SeuratObject]{FetchData}}.
}
\examples{
library(pixelatorR)

se <- ReadPNA_Seurat(minimal_pna_pxl_file(), verbose = FALSE)
se <- LoadCellGraphs(se, cells = colnames(se)[1], add_layouts = TRUE, verbose = FALSE)
cg <- CellGraphs(se)[[1]]

# Coordinates only
layout <- ExtractLayout(cg)

# Include marker counts
layout <- ExtractLayout(cg, vars = "B2M")

# Combine layouts from a CellGraphList
cgl <- CellGraphs(se)
layout <- ExtractLayout(cgl, cells = colnames(se)[1], vars = "B2M")

# PNAAssay method
layout <- ExtractLayout(se[["PNA"]], cells = colnames(se)[1], vars = "B2M")

# Seurat method
layout <- ExtractLayout(se, cells = colnames(se)[1], vars = "B2M")

}
