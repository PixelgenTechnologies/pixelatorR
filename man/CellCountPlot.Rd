% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/cell_count_plot.R
\name{CellCountPlot}
\alias{CellCountPlot}
\alias{CellCountPlot.data.frame}
\alias{CellCountPlot.Seurat}
\title{Plot cell counts per group}
\usage{
CellCountPlot(object, ...)

\method{CellCountPlot}{data.frame}(
  object,
  group_by = NULL,
  color_by,
  show_count = TRUE,
  flip_axes = FALSE,
  as_frequency = FALSE,
  stack = FALSE,
  ...
)

\method{CellCountPlot}{Seurat}(
  object,
  group_by = NULL,
  color_by,
  show_count = TRUE,
  flip_axes = FALSE,
  as_frequency = FALSE,
  stack = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{data.frame}-like object or a \code{Seurat} object}

\item{...}{Not yet implemented}

\item{group_by}{A column in the object representing a 'character' or 'factor'
to group data by}

\item{color_by}{A column in the object representing a 'character' or 'factor'
to color data by}

\item{show_count}{Place the count on top of the bar or next to the bar if
\code{flip_axes = TRUE}}

\item{flip_axes}{Flip the plot layout}

\item{as_frequency}{Plot frequencies instead of counts}

\item{stack}{Create a stacked bar plot. Only has an effect if a \code{group_by}
variable is provided.}
}
\value{
A \code{ggplot} object
}
\description{
QC plot function used to get a quick overview of called cells in an
MPX data set.
}
\examples{

library(pixelatorR)

# Load example data as a Seurat object
pxl_file <- system.file("extdata/five_cells",
  "five_cells.pxl",
  package = "pixelatorR"
)
seur_obj <- ReadMPX_Seurat(pxl_file)
seur_obj

# Add random labels to color by
seur_obj$labels <- sample(c("A", "B"), size = ncol(seur_obj), replace = TRUE)

# Plot with data.frame and color by labels
CellCountPlot(seur_obj[[]], color_by = "labels")

# Plot with Seurat object
CellCountPlot(seur_obj, color_by = "labels")

# Color by sample in merged data
seur_obj1 <- seur_obj2 <- seur_obj
seur_obj1$sample <- "1"
seur_obj2$sample <- "2"
seur_obj_merged <- merge(seur_obj1, seur_obj2, add.cell.ids = c("A", "B"))
CellCountPlot(seur_obj_merged, group_by = "labels", color_by = "sample")

}
\seealso{
Other QC-plots: 
\code{\link{TauPlot}()}
}
\concept{QC-plots}
\concept{plots}
