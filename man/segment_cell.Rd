% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_segmentation.R
\name{segment_cell}
\alias{segment_cell}
\title{cell:cell conjugate segmentation}
\usage{
segment_cell(
  cg,
  w,
  k = 2L,
  detect_interface = TRUE,
  k_interface_expansion = 0L,
  keep_largest_comp = TRUE,
  min_comp_size = 10,
  spatial_smoothing_iter = 5L,
  verbose = TRUE
)
}
\arguments{
\item{cg}{A \code{CellGraph} object}

\item{w}{A matrix of NMF weights with rows corresponding to proteins and columns
corresponding to the two interacting cell types.}

\item{k}{The number of hops to use when computing local node abundance profiles used to
compute the NNLS projection scores.}

\item{detect_interface}{Whether to identify the interface between the two cell types.}

\item{k_interface_expansion}{The number of hops to expand the interface between the two cell types.
The default (0) classifies nodes that are directly connected to both cell types as interface.
Values greater than 0 can be set to expand the interface further by including nodes that are within
k hops of the initial interface nodes.}

\item{keep_largest_comp}{Whether to keep only the largest connected component in each
segmented cell-type graph. If \code{FALSE}, all connected components with at least
\code{min_comp_size} nodes are kept.}

\item{min_comp_size}{The minimum size of connected components to keep when
\code{keep_largest_comp} is \code{FALSE}.}

\item{spatial_smoothing_iter}{The number of iterations to perform for spatial smoothing of
node neighborhoods scores. This helps smoothen out local noise in the graph and improve classification.}

\item{verbose}{Print updates during the segmentation process.}
}
\value{
A \code{CellGraph} object containing the compartment labels from the segmentation as a node attribute.
}
\description{
This function performs segmentation of a cell:cell conjugate graph into its respective
cell types and the interface between them (optional).
}
\details{
Requirements:
\itemize{
\item A \code{CellGraph} object containing the graph representation of the cell:cell conjugate. Only
two cell types should be present in the graph.
\item A matrix of NMF weights (\code{w}) for the two cell types, which can be derived using the
\code{cc_protein_weights} function.
}

The segmentation is performed in several steps:
\enumerate{
\item Classify nodes into "cell1" and "cell2" using Non-Negative Least Squares (NNLS). This
method is described in detail below. After classification, the Largest Connected Component (LCC) is
kept for each cell type to remove small disconnected subgraphs.
\item (Optional) Identify the interface nodes between the two cell types. These are defined as nodes
that are directly connected to both cell types. Optionally, the interface can be expanded
by including neighboring nodes within \code{k_interface_expansion} hops. By setting \code{detect_interface}
to \code{FALSE}, the interface detection step is skipped.
}

Nodes that cannot be classified with confidence are labeled as "other".
}
\section{Why prefer \code{segment_cell} over \code{patch_detection}}{

For cell:cell conjugates, \code{segment_cell()} is preferred over
\code{\link{patch_detection}}. \code{patch_detection()} looks for small foreign
subgraphs (patches) on a dominant receiver cell using a short list of
patch-specific markers. That model does not fit conjugates, where two cells
of comparable size occupy one graph and the relevant structure is the two
cell-type compartments plus their interface.

\code{segment_cell()} instead projects k-hop neighborhood profiles onto
NMF-derived protein weights for both cell types (NNLS), with optional spatial
smoothing. This uses the full two-population signature rather than a handful
of exclusive markers, is less sensitive to marker choice, labels both
compartments symmetrically, and can annotate interface nodes. Use
\code{patch_detection()} when the biology is genuinely patch-like (for example a
small transferred fragment or platelet debris on a larger cell).
}
\section{NNLS method for classifying membrane nodes}{

To segment cell:cell conjugates, we first need to fit an NMF model to the abundance matrix (or local node
neighborhood abundance profiles) of the two cell populations using the \code{cc_protein_weights} function.
This gives us the protein weights for each cell type (\code{w}) that we can use for classification. As a rule of thumb,
a good NMF model should have high weights for proteins that are mutually exclusive between the two cell types.
For instance, if we are segmenting a B cell:T cell conjugate, we would expect the NMF model to give high weights
to CD10, CD20 and CD21 for the B cell identity and CD3e, CD2 and CD5 for the T cell identity.
For the membrane segmentation step, we create a neighborhood profile for each node by summing the abundance
of its neighbors in the graph. We then project the NMF weights (\code{w}) onto these neighborhood profiles using
NNLS to get a score for each node and cell type.
}

\examples{
library(dplyr)
se <- ReadPNA_Seurat(minimal_pna_pxl_file()) \%>\%
  LoadCellGraphs(cells = colnames(.)[2], verbose = FALSE)
cg <- CellGraphs(se)[[2]]

# Get cell type weights
se$cell_type <- c("Mono", "pDC", "CD4T", "CD4T", "CD4T")
w <- cc_protein_weights(
  se,
  group_by = "cell_type",
  population_1 = "Mono", population_2 = "CD4T",
  show_plot = FALSE
)

cg <- segment_cell(
  cg,
  w = w
)

}
\seealso{
\code{\link[=patch_detection]{patch_detection()}}, \code{\link[=cc_protein_weights]{cc_protein_weights()}}
}
