% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objects.R
\name{CreateCellGraphAssay}
\alias{CreateCellGraphAssay}
\title{Create a CellGraphAssay object}
\usage{
CreateCellGraphAssay(
  counts,
  cellgraphs,
  polarization = NULL,
  colocalization = NULL,
  arrow_dir = NULL,
  outdir = NULL,
  overwrite = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{counts}{Unnormalized data (raw counts)}

\item{cellgraphs}{A named list of \code{\link{CellGraph}} objects}

\item{polarization}{A \code{tbl_df} with polarization scores}

\item{colocalization}{A \code{tbl_df} with colocalization scores}

\item{arrow_dir}{A path to an existing directory}

\item{outdir}{A path to an existing directory}

\item{overwrite}{If the output directory already exists, set this parameter
to TRUE to overwrite the directory}

\item{verbose}{Print messages}

\item{...}{Additional arguments passed to \code{\link{CreateAssayObject}}}
}
\value{
A \code{CellGraphAssay} object
}
\description{
Create a \code{\link{CellGraphAssay}} object from a count matrix. The expected
format of the input matrix is features x cells.
}
\examples{

library(pixelatorR)
library(dplyr)
library(tidygraph)

pxl_file <- system.file("extdata/PBMC_10_cells",
                        "Sample01_test.pxl",
                        package = "pixelatorR")
counts <- ReadMPX_counts(pxl_file)
edgelist <- ReadMPX_item(pxl_file, items = "edgelist")
components <- colnames(counts)
edgelist_split <-
  edgelist \%>\%
  select(upia, upib, component) \%>\%
  distinct() \%>\%
  group_by(component) \%>\%
  group_split() \%>\%
  setNames(nm = components)

# Convert data into a list of CellGraph objects
bipartite_graphs <- lapply(edgelist_split, function(x) {
  x <- x \%>\% as_tbl_graph(directed = FALSE)
  x <- x \%>\% mutate(node_type = case_when(name \%in\% edgelist$upia ~ "A", TRUE ~ "B"))
  attr(x, "type") <- "bipartite"
  CreateCellGraphObject(cellgraph = x)
})

# Create CellGraphAssay
cg_assay <- CreateCellGraphAssay(counts = counts, cellgraphs = bipartite_graphs)
cg_assay

}
\concept{assay}
